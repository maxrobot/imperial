#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
	// Global Variables #############################################
	int Q_(9);                  // lattice number of speeds
	double *ex_(0), *ey_(0);     // lattice speed coordinates
	double *w_(0);              // lattice weights
	double c
	int Nx_g(0), Ny_g(0);

	return 0;
}